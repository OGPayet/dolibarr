diff a/htdocs/custom/requestmanager/createfast.php b/htdocs/custom/requestmanager/createfast.php	(rejected hunks)
@@ -279,6 +279,12 @@ $selectedFkUrgency = GETPOST('urgency', 'int') ? intval(GETPOST('urgency', 'int'
 $selectedRequesterNotification = GETPOST('notify_requester_by_email', 'int') > 0 ? 1 : 0;
 $origin = GETPOST('origin', 'alpha');
 $originid = GETPOST('originid', 'int');
+$notify_assigned = GETPOST('notify_assigned','int');
+$notify_requesters = GETPOST('notify_requesters','int');
+$notify_watchers = GETPOST('notify_watchers','int');
+$message_type = GETPOST('message_type','int');
+$message_subject = GETPOST('message_subject','alpha');
+$message = GETPOST('message','alpha');
 
 if ($selectedSocIdOrigin === '' && $selectedSocId > 0) {
     $selectedSocIdOrigin = $selectedSocId;
@@ -340,7 +346,6 @@ if (!empty($selectedRequesterNotification)) $formquestion[] = array('type' => 'h
 if (!empty($origin)) $formquestion[] = array('type' => 'hidden', 'name' => 'origin', 'value' => $origin);
 if (!empty($originid)) $formquestion[] = array('type' => 'hidden', 'name' => 'originid', 'value' => $originid);
 if (!empty($force_out_of_time_confirmed)) $formquestion[] = array('type' => 'hidden', 'name' => 'force_out_of_time_confirmed', 'value' => $force_out_of_time_confirmed ? 1 : 0);
-if (!empty($force_principal_company_confirmed)) $formquestion[] = array('type' => 'hidden', 'name' => 'force_principal_company_confirmed', 'value' => $force_principal_company_confirmed ? 1 : 0);
 
 $formconfirm = '';
 if ($action == 'force_principal_company') {
@@ -424,7 +429,12 @@ $out .= '       socid_watcher: ' . json_encode($selectedSocIdWatcher) . ',';
 $out .= '       source: ' . json_encode($selectedFkSource) . ',';
 $out .= '       type: ' . json_encode($selectedFkType) . ',';
 $out .= '       urgency: ' . json_encode($selectedFkUrgency) . ',';
-$out .= '       notify_requester_by_email: ' . json_encode($selectedRequesterNotification) . ',';
+$out .= '       notify_assigned: ' . json_encode($notify_assigned) . ',';
+$out .= '       notify_requesters: ' . json_encode($notify_requesters) . ',';
+$out .= '       notify_watchers: ' . json_encode($notify_watchers) . ',';
+$out .= '       message_type: ' . json_encode($message_type) . ',';
+$out .= '       message_subject: ' . json_encode($message_subject) . ',';
+$out .= '       message: ' . json_encode($message) . ',';
 $out .= '       zone: 1';
 $out .= '   };';
 $out .= '   var requestManagerLoader = new RequestManagerLoader(0, "create_fast_zone", "' . dol_buildpath('/requestmanager/tpl/rm_createfastzone.tpl.php', 1) . '", ajaxData);';
